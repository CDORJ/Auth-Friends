const initialState = {
  selectedFriend: {},
  showUpdate: false,
};

export const singleFriendReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

